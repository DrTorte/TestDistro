language: node_js
sudo: true
install: true
script: echo "Wieee!"

before_install:
    - npm i -g npm@6.2.0
    - node --version
    - npm --version

jobs:
    include:
      -script: echo "Hello!"
      -script: echo "Not Hello!"
